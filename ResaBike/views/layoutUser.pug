doctype html
html
head
    title= "ResaBike"
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/css/materialize.min.css")
    link(href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet")
    link(rel='stylesheet', href='/stylesheets/style.css')
    script(src="https://code.jquery.com/jquery-3.2.1.min.js")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/js/materialize.min.js")
    script(src="/javascripts/components/search-it.min.js" type="text/javascript")
    script.
      var langUsed = "#{langUsed}";
      var lang = "#{JSON.stringify(lang)}";
      lang = JSON.parse(lang.replace(/&quot;/g,'"'));
body

    //- Navbar for the admin part

    nav.nav-extended
        .nav-wrapper
            a.brand-logo(href="#") Bienvenue #{currentUser.username}
            a(href="#mobile-demo", data-activates="mobile-demo" class="button-collapse")
                i.material-icons menu
            //- Premier UL pour la navbar normal
            ul(id="nav-mobile" class="right hide-on-med-and-down")
                if(currentUser.RoleId == 3)
                    li
                        a(href="/user") Users
                    li
                        a(href="/zone") Zones
                    li
                        a(href="/bookings") Reservations
                    li
                        a(href="/user/logout") Logout
                else
                    li
                        a(href=`/zone/${currentUser.ZoneId}`) Zones
                    li
                        a(href=`/booking/s${currentUser.ZoneId}`) Reservations
                    li
                        a(href="/user/logout") Logout
            //- Deuxieme ul pour le navbar mobile
            ul(id="mobile-demo", class="side-nav")
                if(currentUser.RoleId == 3)
                    li
                        a(href="/user") Users
                    li
                        a(href="/zone") Zones
                    li
                        a(href="/bookings") Reservations
                    li
                        a(href="/user/logout") Logout
                else
                    li
                        a(href=`/zone/${currentUser.ZoneId}`) Zones
                    li
                        a(href=`/bookings/${currentUser.ZoneId}`) Reservations
                    li
                        a(href="/user/logout") Logout
                


    block content